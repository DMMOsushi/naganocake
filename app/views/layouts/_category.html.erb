<div class="col-3">
	<table class="table table-bordered table-hover">
		<thead>
			<tr>
				<th>ジャンル検索</th>
			</tr>
		</thead>
		<tbody>
			<% categories.each do |genre| %>
				<tr>
					<td style="border: none"><%= link_to category.name, items_path(:category_id => category.id) %></td>
				</tr>
			<% end %>
		</tbody>
	</table>
</div>